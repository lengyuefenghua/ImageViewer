<ui:FluentWindow x:Class="ImageViewer.Views.CopyToWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="复制到" Icon="/ImageViewer.ico" Width="860" Height="540" MinWidth="720" MinHeight="420"
                 WindowStartupLocation="CenterOwner" Background="{DynamicResource ApplicationBackgroundBrush}" ShowInTaskbar="False"
                 ExtendsContentIntoTitleBar="True"
                 PreviewKeyDown="OnPreviewKeyDown" Closing="OnClosing">
  <DockPanel>
    <ui:TitleBar DockPanel.Dock="Top" Background="{DynamicResource LayerFillColorDefaultBrush}" Height="30" FontSize="15" Padding="0"
                 ShowMinimize="False" ShowMaximize="False"
                 Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}">
      <ui:TitleBar.Icon>
        <ui:ImageIcon Source="pack://application:,,,/ImageViewer.ico" />
      </ui:TitleBar.Icon>
    </ui:TitleBar>

    <Grid Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- 工具行：左侧行操作，右侧全局设置 -->
      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal">
          <ui:Button Content="新增" Margin="0,0,6,0" Click="AddClick" />
          <ui:Button Content="删除" Margin="0,0,6,0" Click="RemoveClick" />
          <ui:Button Content="上移" Margin="0,0,6,0" Click="MoveUpClick" />
          <ui:Button Content="下移" Click="MoveDownClick" />
        </StackPanel>
        <StackPanel Grid.Column="2" Orientation="Horizontal">
          <ui:TextBlock Text="时间戳格式" VerticalAlignment="Center" />
          <ComboBox x:Name="TimestampFormatComboBox" Width="170" MinHeight="32" Padding="10,5,10,5" Margin="8,0,16,0" VerticalAlignment="Center" />
          <ui:TextBlock Text="同名冲突" VerticalAlignment="Center" />
          <ComboBox x:Name="ConflictComboBox" Width="100" MinHeight="32" Padding="10,5,10,5" Margin="8,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
      </Grid>

      <!-- 表头：列宽与下方行模板保持一致 -->
      <Grid Grid.Row="1" Margin="0,14,0,4">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="34" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="140" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ui:TextBlock Grid.Column="0" Text="序号" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <ui:TextBlock Grid.Column="1" Text="目录路径" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <ui:TextBlock Grid.Column="3" Text="后缀" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <ui:TextBlock Grid.Column="4" Text="操作" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
      </Grid>

      <ListBox x:Name="TargetList" Grid.Row="2" Background="Transparent" BorderThickness="0"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               ScrollViewer.VerticalScrollBarVisibility="Auto"
               PreviewMouseLeftButtonUp="TargetListMouseUp">
        <ListBox.Resources>
          <!-- 选中项用低调半透明白，替代主题默认的高亮蓝。 -->
          <SolidColorBrush x:Key="ListBoxItemSelectedBackgroundThemeBrush" Color="{DynamicResource SubtleFillColorSecondary}" />
        </ListBox.Resources>
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
          </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid Margin="0,3">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="34" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ui:TextBlock Grid.Column="0" Text="{Binding Number}" VerticalAlignment="Center" />
              <TextBox Grid.Column="1" VerticalContentAlignment="Center"
                       Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}" />
              <ui:Button Grid.Column="2" Content="浏览…" Margin="6,0,0,0" Click="BrowseClick" Tag="{Binding}" />
              <TextBox Grid.Column="3" Margin="6,0,0,0" VerticalContentAlignment="Center" ToolTip="后缀（原样拼在扩展名前）"
                       Text="{Binding Suffix, UpdateSourceTrigger=PropertyChanged}" />
              <ui:Button Grid.Column="4" Content="复制" Margin="6,0,0,0" Click="CopyRowClick" Tag="{Binding}" />
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <ui:TextBlock Grid.Row="3" x:Name="StatusText" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
        <ui:TextBlock.Style>
          <Style TargetType="ui:TextBlock">
            <!-- 无消息时不占高度，避免留白。 -->
            <Style.Triggers>
              <Trigger Property="Text" Value="">
                <Setter Property="Visibility" Value="Collapsed" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </ui:TextBlock.Style>
      </ui:TextBlock>
    </Grid>
  </DockPanel>
</ui:FluentWindow>
